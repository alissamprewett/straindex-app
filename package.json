{
  "name": "straindex",
  "version": "1.0.0",
  "description": "StrainDex — a social check-in, strain library, and recipe app. Uses only Node's built-ins plus a small set of dependencies (@libsql/client for its Turso database, @sentry/node for error reporting, @aws-sdk/client-s3 for check-in photo storage on Cloudflare R2).",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node --no-warnings --require ./instrument.js server.js",
    "seed": "node --no-warnings seed.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@libsql/client": "^0.17.0",
    "@sentry/node": "^10.70.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
