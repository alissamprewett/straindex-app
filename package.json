{
  "name": "straindex",
  "version": "1.0.0",
  "description": "StrainDex — a social check-in, strain library, and recipe app. Zero-dependency Node.js app (no npm packages required).",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node --no-warnings server.js",
    "seed": "node --no-warnings seed.js"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
